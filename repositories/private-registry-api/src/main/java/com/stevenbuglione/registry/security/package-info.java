/** Authentication, identity resolution, and fail-closed entitlement enforcement. */
@ApplicationModule(
    displayName = "Security",
    allowedDependencies = {})
@NullMarked
package com.stevenbuglione.registry.security;

import org.jspecify.annotations.NullMarked;
import org.springframework.modulith.ApplicationModule;
