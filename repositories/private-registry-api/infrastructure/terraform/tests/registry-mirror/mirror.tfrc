provider_installation {
  network_mirror {
    url     = "https://artifacts.example.invalid/artifactory/api/terraform/iac-providers-public-remote/providers/"
    include = ["registry.terraform.io/hashicorp/null"]
  }

  direct {
    exclude = ["registry.terraform.io/hashicorp/null"]
  }
}
